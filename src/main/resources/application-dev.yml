spring:
  application:
    name: 'Cyno-Desu'
  datasource:
    url: jdbc:h2:mem:testdb-dev
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  port: 8081

logging:
    level:
      org.springframework.security: DEBUG
      thymeleaf: DEBUG